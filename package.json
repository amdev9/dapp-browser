{
  "name": "larkdapp",
  "version": "1.0.0",
  "description": "An application to run dapp apps in a sandbox",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron-reload": "^1.2.2",
    "node-dir": "^0.1.17",
    "on-change": "^0.1.0"
  }
}
