{
  "name": "array-io-client",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "setup": "concurrently --kill-others \"cd electron/client && npm i && cross-env NODE_ENV=development node --max_old_space_size=2096 server.js\" \"cd electron/dapps/lib && npm i && npm run webpack\" \"cd electron/main && npm i && npm run rebuild && npm run webpack\"",
    "dev": "NODE_ENV=development npm start",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "cypherpunk99",
  "license": "MIT",
  "dependencies": {
    "@types/redux": "^3.6.0",
    "@types/redux-logger": "^3.0.6",
    "concurrently": "^4.0.1",
    "cross-env": "^5.2.0",
    "react-burger-menu": "^2.5.2",
    "react-perfect-scrollbar": "^1.2.0",
    "redux": "^4.0.0",
    "redux-logger": "^3.0.6",
    "sqlite3": "^4.0.2"
  }
}
